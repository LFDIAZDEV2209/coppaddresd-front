# Configuración de entorno para el frontend de CoppAddresd.
# Copia este archivo a `.env.local` y ajusta los valores.

# URL del microservicio de autenticación (Auth Service)
NEXT_PUBLIC_AUTH_API_URL=http://localhost:5123

# URL de la API principal (CoppAddresd.Api)
NEXT_PUBLIC_API_URL=http://localhost:5122

# URL del microservicio de Telemedicina (schema tele)
NEXT_PUBLIC_TELEMEDICINE_API_URL=http://localhost:5130

# Minutos de inactividad antes de cerrar la sesión (default: 30)
NEXT_PUBLIC_SESSION_IDLE_MINUTES=30